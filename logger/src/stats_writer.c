#include <stats_writer.h>
#include <hash_map.h>


/* void parse_file(int fd, t_hashmap *hashmap)
{
    char *line;
    
    line = get_next_line(fd);
    if (!line)
        return ;
    while(line)
    {
        
    }
} */